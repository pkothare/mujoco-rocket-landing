[project]
name = "rocket-sim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.8.0",
    "moviepy>=1.0.3",
    "mujoco>=3.4.0",
    "torch>=2.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.2",
]
